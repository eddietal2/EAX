<script lang="ts">
	import { toastStore } from '$lib/stores/toast';
</script>

<div class="fixed bottom-20 md:bottom-6 right-4 z-50 flex flex-col gap-2 pointer-events-none">
	{#each $toastStore as toast (toast.id)}
		<div
			class="pointer-events-auto px-4 py-3 rounded-lg shadow-lg font-medium text-sm transition-all duration-300 animate-slide-in flex items-center gap-2 border"
			class:bg-emerald-50={toast.type === 'success'}
			class:text-emerald-800={toast.type === 'success'}
			class:border-emerald-200={toast.type === 'success'}
			class:bg-red-50={toast.type === 'error'}
			class:text-red-800={toast.type === 'error'}
			class:border-red-200={toast.type === 'error'}
			class:bg-blue-50={toast.type === 'info'}
			class:text-blue-800={toast.type === 'info'}
			class:border-blue-200={toast.type === 'info'}
		>
			{#if toast.type === 'success'}
				<svg class="w-5 h-5 flex-shrink-0" fill="currentColor" viewBox="0 0 20 20">
					<path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd" />
				</svg>
			{:else if toast.type === 'error'}
				<svg class="w-5 h-5 flex-shrink-0" fill="currentColor" viewBox="0 0 20 20">
					<path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zM8.707 7.293a1 1 0 00-1.414 1.414L8.586 10l-1.293 1.293a1 1 0 101.414 1.414L10 11.414l1.293 1.293a1 1 0 001.414-1.414L11.414 10l1.293-1.293a1 1 0 00-1.414-1.414L10 8.586 8.707 7.293z" clip-rule="evenodd" />
				</svg>
			{:else}
				<svg class="w-5 h-5 flex-shrink-0" fill="currentColor" viewBox="0 0 20 20">
					<path fill-rule="evenodd" d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-7-4a1 1 0 11-2 0 1 1 0 012 0zM9 9a1 1 0 000 2v3a1 1 0 001 1h1a1 1 0 100-2v-3a1 1 0 00-1-1H9z" clip-rule="evenodd" />
				</svg>
			{/if}
			<span>{toast.message}</span>
		</div>
	{/each}
</div>

<style>
	@keyframes slide-in {
		from {
			transform: translateX(400px);
			opacity: 0;
		}
		to {
			transform: translateX(0);
			opacity: 1;
		}
	}

	:global(.animate-slide-in) {
		animation: slide-in 0.3s ease-out;
	}
</style>
